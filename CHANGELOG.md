# 更新记录

## 2026-09-12

### 文档与当前功能

- 补充详情页固定底部操作区、显示原图按钮和独立滚动区域说明。
- 补充重复图片与怀疑图片的判定边界、自动勾选规则和源文件删除行为。
- 补充任务状态栏、操作日志和 Docker 源码构建/升级说明。
- 统一说明增量扫描、近屏加载、预热缓存、SSE 断连资源释放和低 IO 设计。

## 2026-09-09

### 性能与 IO

- 增量扫描使用 `path + mtime_ns + size` 判断文件是否变化。
- 全量扫描保留目录遍历和缺失文件清理，但未变化文件不再重复写数据库。
- 普通扫描不逐文件计算 hash；仅在同步元数据确认“同一作品、同一页”存在多个本地文件时计算 SHA-256。
- watchdog 默认使用事件监听和 30 秒防抖，不启用轮询时不会定时访问图片目录。
- 扫描入库、缩略图生成和元数据同步使用有界队列与小批量提交。
- SQLite 使用 WAL、`synchronous=NORMAL` 和 busy timeout，降低后台任务与页面查询的锁冲突。
- 画廊使用近屏懒加载，图片进入上下约两个屏幕范围时才开始加载。
- 恢复画廊模式切换预热缓存，按当前排序/筛选预热少量目标模式图片；显示窗口关闭达到设定时间后自动释放。

### 新增功能

- 支持多个本地图片目录。
- 支持自动监看目录并在扫描后自动刷新前端。
- 支持增量扫描、增量元数据同步和设置页全量扫描/重建元数据。
- 支持 Pixiv AI 状态识别、AI 筛选和整理路径分类。
- 支持严格 R18 标签筛选。
- 新增重复图片页面、自动勾选较晚文件、源文件删除和去重日志。
- 新增整理文件页面，支持多源目录、Shaft 路径模板、自定义模板、复制、移动、软链接、硬链接、自动重命名和撤销。
- 整理路径自定义模板现在只负责目录；文件名变量和文件名规则统一放入重命名模板，默认保持旧版原文件名行为。
- 新增整理日志、设置页操作日志、任务暂停/继续/取消和失败自动暂停。
- 新增查看器文件夹入口开关、打开详情页自动显示原图开关和侧边栏悬停展开开关。
- 新增收藏订阅及按周定时自动更新。

### 修复与一致性

- 多页作品按文件名中的 `p1/p2/p3` 排序，缩略图固定取第一页。
- 修复旧数据库缺少 `file_name`、文件 stat、AI 字段时的迁移问题。
- 修复重复文件删除只删数据库、不删源文件的问题。
- 整理关闭自动重命名时保留源文件名。
- 修复 AI 字段为字符串时无法正确筛选和整理的问题。
- 修复全量扫描路径判断可能误处理相邻目录的问题。

## 1.2.1

- 收藏订阅、局域网访问、直连/代理模式、自动监看和多页作品显示等功能的基础版本。
